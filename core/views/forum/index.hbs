{{!< ../layout}}
{{#content "pageStyles"}}
{{!-- page css --}}
{{/content}}
{{#content "pageScript"}}
<script src="/javascripts/forum/index.js"></script>
{{/content}}

<div class="main">
    {{!-- category --}}
    {{#each categorys}}
    <div class="forum-category">
        <h4 style="background:{{bcolor}}">{{name}}</h4>
        {{!-- channel --}}
        <div class="forum-channel">
            {{#each channels}}
            <div class="item" style="background:{{bcolor}};">
                <a class="name" href="/channel/{{id}}">{{name}}</a>
                <div class="count">{{count}}</div>
                <div>
                    <div class="icon"></div>
                </div>
                <div class="lastpost" style="display:none;">
                    <a class="lastpost-title" href="/topic/{{lastpost.id}}">
                        {{lastpost.title}}
                    </a>
                    <a class="lastpost-author" href="/user/{{channel.lastpost.author.id}}">
                        {{lastpost.author.name}}
                    </a>
                    <div class="lastpost-time">
                        {{lasttime}}
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        <div class="clear"></div>
    </div>
    {{/each}}
</div>